services:
  postgres:
    ports:
      - "127.0.0.1:5432:5432"

  bot:
    volumes:
      - ./app:/app/app:ro
    environment:
      LOG_FORMAT: text
      PYTHONDONTWRITEBYTECODE: "1"
    ports:
      - "127.0.0.1:8080:8080"
      - "127.0.0.1:9090:9090"
    restart: "no"
